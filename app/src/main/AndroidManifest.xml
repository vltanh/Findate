<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.narubibi.findate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/progressbar_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/progressbar_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Authentication.ChooseLoginRegisterActivity"></activity>
        <activity android:name=".Activities.Authentication.RegisterActivity" />
        <activity android:name=".Activities.Authentication.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="._Match.MatchesActivity" />
        <activity android:name="._Chat.ChatActivity"></activity>
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/> <!-- optional (needed if default theme has no action bar) -->

        <activity android:name=".Activities.MainActivity" android:windowSoftInputMode="stateAlwaysHidden|adjustPan|adjustResize">
        </activity>
        <activity android:name=".Activities.SettingActivity"/>

        <activity android:name=".Activities.EditInfoActivity"
            android:screenOrientation="portrait">
        </activity>

    </application>

</manifest>